# Production Docker Compose — configured in Phase 9.
# This file will add: pre-built image references, Caddy reverse proxy,
# HTTPS termination, resource limits, restart policies, and log drivers.

services: {}
